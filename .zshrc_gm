# Variables
export HTTP_PROXY="http://127.0.0.1:3128"
export HTTPS_PROXY="$HTTP_PROXY"
export NO_PROXY="TODO"

export SONAR_TOKEN="{{ .data.gm.sonarToken }}"
export GITHUB_TOKEN="{{ .data.gm.githubToken }}"

# Sources
source <(kapp completion zsh)
